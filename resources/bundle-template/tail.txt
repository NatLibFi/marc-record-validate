
    );
  }

}(this, factory));

function factory()
{

  'use strict';

  var args = [];

  for (var i = 0; i < arguments.length; i++) {
    args.push(arguments[i]);
  }

  return arguments[0].call(args);

}
